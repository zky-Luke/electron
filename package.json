{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "packager": "electron-packager ./ HelloWorld --platform=win32 --arch=all --out ./outApp --electronVersion 1.0.1 --overwrite --icon=./img/electron.ico"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-prebuilt": "^1.4.13"
  },
  "devDependencies": {
    "electron-packager": "^8.7.2"
  }
}
